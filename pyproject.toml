[project]
name = "crossword"
version = "0.1.0"
description = ""
authors = [
    {name = "Federico Mora",email = "fmorarocha@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "yattag (>=1.16.1,<2.0.0)",
    "typer (>=0.15.2,<0.16.0)",
    "openai (>=1.65.2,<2.0.0)",
    "pydantic (>=2.10.6,<3.0.0)",
    "z3-solver (==4.11.2.0)",
    "setuptools (>=75.8.2,<76.0.0)",
    "gdown (>=5.2.0,<6.0.0)"
]

[project.scripts]
crossword = "crossword:app"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

